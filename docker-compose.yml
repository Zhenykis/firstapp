version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: TestInlyIT
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mydatabase
    ports:
      - "5432:5432"


